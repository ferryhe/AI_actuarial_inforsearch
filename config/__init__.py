"""Python settings package for optional app components.

Note: This repository also uses the `config/` directory to store YAML configuration
files (e.g. `config/sites.yaml`). Adding this package does not affect reading those
files by path; it only enables imports like `from config.settings import get_settings`.
"""

