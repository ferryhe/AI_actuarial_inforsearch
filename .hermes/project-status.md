# Project Status

- Date: 2026-05-23
- Branch: codex/fix-knowledge-deepseek-database-explain
- Scope: Knowledge/KB creation and detail flow, chunk profile task flow, RAG indexing task UI, Database-to-Chat document explanation, DeepSeek defaults, Database header cleanup, Chat document sidebar filters, and Flask-to-React parity GAP analysis.
- Latest baseline: origin/main at d05198e729456c7830830e7dc0babdf834ba0a0e after PR #113 merge and successful Gitee sync follow-up.
- Knowledge page: The create-KB panel no longer exposes embedding selection, inline chunk size, or overlap settings. It now requires an existing chunk profile, filters manual document choices to files with ready chunk sets for that profile, supports document and multi-category selection, shows category readiness stats, and sends `chunk_profile_id`, `file_urls`, and `categories` to the API. It also supports opening create mode from `/knowledge?open=create` and a create-and-index action.
- KB detail page: Bindable document search is scoped to the KB's chunk profile and posts exact chunk-set bindings so existing chunks are linked into the KB rather than regenerated.
- RAG admin API: `POST /api/rag/knowledge-bases` accepts `chunk_profile_id`, persists the KB/profile link, derives KB chunk metadata from that profile, ignores arbitrary request chunk size/overlap when a profile is supplied, adds only files with existing ready chunk sets for that profile, and binds those chunk sets in `follow_latest` mode. `/api/rag/files/selectable` can filter by `profile_id` and returns chunk set metadata for the selected profile. Added `/api/rag/categories/stats` for category readiness previews and made explicit chunk bindings also add KB file membership.
- RAG category mapping: Local UI click testing found `GET /api/rag/categories/mapping` could 500 on a clean FastAPI DB without legacy `source_metadata`. The route now reads categories from `catalog_items` and merges legacy `source_metadata` only when that table exists.
- Tasks page: Restored a RAG indexing task card and form backed by the existing `rag_indexing` collection API, added a create-KB task entry that opens Knowledge create mode, and reordered cataloging after Markdown generation.
- Chunk tasks: The chunk generation form now defaults to existing chunk profiles, offers a deliberate custom-profile mode for new profiles, and can optionally bind generated chunks into an existing KB. Runtime chunk generation now filters existing chunks by selected profile and can bind generated chunk sets to a KB.
- Database page: Restored the AI explain action for markdown-backed files and removed the top-right "FastAPI native file operations" badge.
- Chat document explanation: Chat posts `document_content`, `document_filename`, and `document_file_url` when given a document from Database, avoiding retrieval fallback for direct document explanation.
- Chat document sidebar: The Chat document tab now shows document rows by filename only, replaces the free-text category filter with a category chip multi-select sourced from used categories, and sends repeated `category` query parameters to `/api/chat/available-documents`. The API now treats repeated/comma-separated `category` and `categories` filters as OR conditions while preserving semicolon-separated multi-category matching.
- DeepSeek config: Verified official DeepSeek docs list OpenAI-compatible `base_url` as `https://api.deepseek.com` and current model IDs as `deepseek-v4-flash` and `deepseek-v4-pro`; updated runtime/discovery defaults from `/v1` to the official root endpoint while keeping legacy aliases.
- Flask-to-React GAP analysis: Three read-only subagents compared the archived Flask branch (`origin/archive/flask-only-system`) against current React/FastAPI for file detail/preview/database actions, tasks/task forms, and settings. Highest-priority follow-ups identified: restore FileDetail AI Explain, fix chat citation links to React routes, restore rich Markdown rendering and full chunk profile/KB binding controls in FileDetail, add RAG index `file_urls`, add global schedule/defaults visibility, fix Settings category save to include `ai_keywords`, and expose existing provider/search/backup maintenance endpoints in Settings.
- Verification: `python -m pytest tests/test_tasks_react_source.py tests/test_knowledge_react_source.py tests/test_database_react_source.py tests/test_fastapi_rag_admin_endpoints.py tests/test_task_stop_support.py::test_native_task_runtime_chunk_generation_uses_existing_service tests/test_task_stop_support.py::test_native_task_runtime_chunk_generation_filters_existing_chunks_by_selected_profile tests/test_task_stop_support.py::test_native_task_runtime_runs_rag_indexing_collection -q` passed (31 passed); `python -m pytest tests/test_chat_react_source.py tests/test_llm_models.py tests/test_ai_runtime.py tests/test_chatbot_core.py tests/test_catalog_runtime.py -q` passed (104 passed); `npm.cmd run build -- --outDir C:/tmp/ai-actuarial-vite-build` passed with the existing large-chunk warning; local Vite HTTP smoke returned 200 for `/knowledge`, `/knowledge?open=create`, `/database`, and `/tasks`; `git diff --check` passed.
- Local button smoke: Started temporary FastAPI/Vite against `C:/tmp/ai-actuarial-ui-clicktest` with seeded files, markdown, chunk profiles, ready chunk sets, KBs, and an admin test session. Clicked Knowledge create/cancel/submit/profile/delete/cleanup/category stats/Create & Index; KB detail save/index/rebuild/pending/bind/cancel/close/remove/category controls; Tasks run/scheduled tabs/create-KB route/chunk existing/custom/bind/schedule/RAG index/log modal/back; Database search/filter/select/sort/preview/page next/prev/jump/AI explain; Chat send after AI explain. The Chat request did not show `Retrieval failed`; it reached provider auth and failed only because the local key was intentionally fake.
- Follow-up verification: `python -m pytest tests/test_fastapi_rag_admin_endpoints.py::test_fastapi_rag_admin_categories_mapping_uses_catalog_items_without_legacy_table tests/test_fastapi_rag_admin_endpoints.py::test_fastapi_rag_admin_category_stats_and_kb_profile_metadata tests/test_fastapi_rag_admin_endpoints.py::test_fastapi_rag_admin_chunk_binding_adds_kb_file_membership -q` passed (3 passed); `python -m pytest tests/test_fastapi_rag_admin_endpoints.py tests/test_knowledge_react_source.py tests/test_tasks_react_source.py tests/test_database_react_source.py tests/test_chat_react_source.py -q` passed (32 passed).
- Chat sidebar verification: Added source/API coverage for filename-only document rows and multi-category filtering. `python -m pytest tests/test_chat_react_source.py tests/test_fastapi_chat_endpoints.py::test_fastapi_chat_conversation_and_catalog_surfaces_work -q` passed (5 passed); `npm.cmd run build -- --outDir C:/tmp/ai-actuarial-chat-build` passed with the existing large-chunk warning; in-app browser smoke opened `/chat`, switched to Documents, confirmed `doc-category-filter` renders and legacy `input-doc-category` is absent. The current local DB had no available document categories, so category click behavior was covered by source/API tests rather than browser data.
- Verification notes: Default `npm.cmd run build` to the repo `dist` path was blocked by sandbox file-system permissions while copying `opengraph.jpg`, so the same Vite build was verified with an output directory under `C:/tmp` using escalated permissions. Browser automation connected but local navigation to `127.0.0.1`/`localhost` was blocked with `net::ERR_BLOCKED_BY_CLIENT`; HTTP route smoke was used for local UI entry verification.
- Pre-PR review gate: Blocked; `codex --help` failed normally and with escalated sandbox permissions with `Program 'codex.exe' failed to run: Access is denied` (reconfirmed after the Chat sidebar follow-up).
- Notes: No sibling repositories were read or modified.
