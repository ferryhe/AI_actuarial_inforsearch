# 2026-03-09 纯 Flask 分支同步测试修复

## 进展摘要

- 目标：将 `feature/react-fastapi-migration` 上验证通过的 4 个共享后端修复同步到 `archive/flask-only-system`。
- 结果：4 个修复已全部 cherry-pick 完成，纯 Flask 分支全量测试恢复通过。
- 本次同步仅涉及共享后端逻辑，不包含 FastAPI 网关和 React/Vite 相关改动。

## 已完成任务

- 同步 RAG schema 兼容、配置缓存隔离、`.env` 测试隔离、conversation token 回退逻辑。
- 同步聊天链路可 patch 化修复，避免测试时提前绑定真实 OpenAI/检索类。
- 同步匿名权限边界修复，保留公共聊天入口，收紧后台配置和管理接口。
- 在 `archive/flask-only-system` 上完成全量 pytest 验证。

## 同步的提交

- `ac724f7` `fix: stabilize schema and config-dependent backend tests`
- `50ac37b` `fix: make chatbot integrations patch-friendly`
- `4d31497` `fix: restrict anonymous access to admin backend APIs`
- `cf821a4` `fix: preserve public chat access in guest mode`

## 自动化验证

```bash
.\.venv\Scripts\python.exe -m pytest -q -o addopts=
```

结果：

- `397 passed`
- `39 warnings`
- `4 subtests passed`

说明：

- 纯 Flask 分支没有 FastAPI 入口测试，所以相较迁移分支少 2 个通过用例，属于预期差异。

## 技术结论

- 这 4 个修复都是共享后端能力，不依赖 FastAPI，因此同步到 Flask-only 分支是合理且必要的。
- 归档分支当前也可作为独立可运行、可测试的纯 Flask 版本保留。
- 如果后续你打算长期保留该分支，建议后续共享后端修复继续按同样方式择要回补。

## 用户测试清单

- [ ] 在 `archive/flask-only-system` 分支启动 Flask 应用，确认首页与数据库页面能访问。
- [ ] 验证匿名用户可进入公共聊天页面，但后台设置页仍需要认证。
- [ ] 验证文件详情里的 catalog 更新和 markdown 读写仍正常。
- [ ] 验证管理员 token 仍可访问设置、调度和 LLM provider 管理接口。

## 下一步

- 如果要继续维护该归档分支，可以把这次同步结果作为分支基线。
- 如果该分支只作为稳定备份，也可以到此为止，不再继续同步 FastAPI 迁移相关变更。
