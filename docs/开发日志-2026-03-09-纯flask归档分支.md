# 开发日志 - 2026-03-09 - 纯 Flask 归档分支

## 进度摘要

本阶段完成了 `archive/flask-only-system` 分支的第一轮整理，目标是从当前双系统基线中剥离 React/Vite 前端，保留一套可独立运行的 Flask 版本作为归档与回退基线。

## 已完成任务

- 删除 React/Vite 前端目录 `client/`
- 删除 Node/Vite 构建文件：
  - `package.json`
  - `package-lock.json`
  - `vite.config.ts`
  - `tsconfig.json`
- 调整 `Caddyfile`，改为所有页面和 API 统一转发到 Flask 服务
- 调整 `README.md`，将本分支说明为纯 Flask 归档分支
- 调整 `.replit` 和 `replit.md`，移除前端工作流和 Node 构建说明

## 技术决策

- 归档分支只做“去 React 化”，不修改 Python 业务逻辑。
- Docker 入口继续沿用 `python -m ai_actuarial web`，确保归档分支仍然是低风险、可回退的 Flask 版本。
- Caddy 在该分支不再代理前端开发服务器，而是统一反代到 Flask 5000 端口。

## 自动化检查

已执行：

- `python -m compileall ai_actuarial`：通过
- `pytest -q -o addopts=`：未能完成

未完成原因：

- 当前环境缺少测试依赖，至少缺 `sqlalchemy`、`openai`、`cryptography`
- 因此 pytest 在收集阶段失败，不属于本次纯 Flask 清理引入的语法错误

## 🧪 用户测试清单

- [ ] 启动 Flask 服务：`python -m ai_actuarial web --host 127.0.0.1 --port 8000`
- [ ] 打开首页 `/`
- [ ] 打开 `/database`、`/tasks`、`/settings`、`/chat`
- [ ] 验证 `/api/stats` 能返回 JSON
- [ ] 验证 `/api/files` 能返回文件列表
- [ ] 验证 Caddy 指向 Flask 后，首页和 `/api/*` 都可访问

## 遇到的问题与处理

- PowerShell 环境下直接删除目录命令被策略阻断，改用 `git rm` 删除受版本控制文件。
- `pytest.ini` 包含覆盖率参数，但本地环境缺 `pytest-cov`，因此改为 `-o addopts=` 方式运行测试。

## 下一步

- 提交纯 Flask 归档分支变更
- 回到基线创建 `feature/react-fastapi-migration`
- 在迁移分支中引入 FastAPI 入口，并先建立兼容层接管现有 Flask API
